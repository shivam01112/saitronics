# Saitronics

Static website for Saitronics technology and business solutions.

## Local preview

Because the site loads shared HTML fragments with JavaScript, serve it over HTTP:

```powershell
py -m http.server 8000
```

Open <http://localhost:8000> in a browser.